// Generated by CoffeeScript 1.9.1
(function() {
  (function($, sr) {
    var debounce;
    debounce = function(func, threshold, execAsap) {
      var debounced, timeout;
      timeout = void 0;
      return debounced = function() {
        var args, delayed, obj;
        delayed = function() {
          if (!execAsap) {
            func.apply(obj, args);
          }
          timeout = null;
        };
        obj = this;
        args = arguments;
        if (timeout) {
          clearTimeout(timeout);
        } else {
          if (execAsap) {
            func.apply(obj, args);
          }
        }
        timeout = setTimeout(delayed, threshold || 100);
      };
    };
    $.fn[sr] = function(fn) {
      if (fn) {
        this.bind("load resize", debounce(fn));
      } else {
        this.trigger(sr);
      }
    };
  })(jQuery, "smartresize");

}).call(this);
